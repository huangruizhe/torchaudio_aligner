# TorchAudio Long-Form Aligner - Core Dependencies
# ================================================
#
# For pip installation with optional dependencies, use:
#   pip install torchaudio-aligner[core]      # Recommended for most users
#   pip install torchaudio-aligner[full]      # All features
#   pip install torchaudio-aligner[nemo]      # NeMo backend
#   pip install torchaudio-aligner[dev]       # Development tools
#
# Or install from this file:
#   pip install -r requirements.txt
#
# See pyproject.toml for full list of optional dependencies.

# ============================================
# Required dependencies
# ============================================
torch>=2.0.0
torchaudio>=2.0.0

# ============================================
# Core optional dependencies (recommended)
# ============================================

# Audio loading backends
torchcodec>=0.1.0      # For torchaudio >= 2.8
soundfile>=0.12.0      # Fallback audio loading

# Text loading and processing
requests>=2.28.0       # URL text loading
beautifulsoup4>=4.11.0 # HTML parsing
pypdf>=3.0.0           # PDF text extraction

# Text normalization (numbers to spoken form)
num2words>=0.5.0

# Romanization for non-Latin scripts
uroman-python>=1.2.0
zhon>=2.0.0            # CJK text processing

# Tokenizers
sentencepiece>=0.2.0   # BPE tokenizer

# ============================================
# Additional tokenizers (optional)
# ============================================
# cmudict>=1.0.0       # CMU pronunciation dictionary
# g2p_en>=2.1.0        # English grapheme-to-phoneme

# ============================================
# Japanese support (optional)
# ============================================
# cutlet>=0.4.0        # Morphological romanization (installs fugashi, unidic)

# ============================================
# OCR for scanned PDFs (optional)
# Requires: brew install poppler (macOS) or apt install poppler-utils (Linux)
# ============================================
# easyocr>=1.7.0
# pdf2image>=1.16.0

# ============================================
# Audio enhancement (optional)
# ============================================
# pyloudnorm>=0.1.0    # Loudness normalization
# demucs>=4.0.0        # Source separation (vocals extraction)
# noisereduce>=3.0.0   # Spectral gating (CPU-friendly)
# deepfilternet>=0.5.0 # Deep learning noise suppression

# ============================================
# Alternative alignment backends (optional)
# ============================================
# nemo_toolkit[asr]>=1.20.0           # NeMo forced aligner
# montreal-forced-aligner>=3.0.0      # MFA (prefer conda install)

# WFST operations - k2 requires manual installation:
# See: https://k2-fsa.github.io/k2/installation.html
# pip install k2 -f https://k2-fsa.org/nightly/

# ============================================
# Development dependencies
# ============================================
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=23.0.0
# ruff>=0.1.0
